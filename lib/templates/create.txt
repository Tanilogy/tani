var root = { root: '#ROOT#', type: '#TYPE#' }
MERGE (n:Root %root%);

MATCH (a:Root %root%), (b:Concept %#GLOBALID#%) MERGE (a)-[:MEANS]->(b);
MATCH (a:Root %root%), (b:Locality %locality%) MERGE (a)-[:USED_AT]->(b);
