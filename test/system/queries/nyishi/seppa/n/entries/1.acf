var root = { entity: 'ñi', type: 'n' }
MERGE (n:Root %root%);

MATCH (a:Root %root%), (b:Concept %N1%) MERGE (a)-[:MEANS]->(b);
MATCH (a:Root %root%), (b:Locality %locality%) MERGE (a)-[:USED_AT]->(b);
