var root = { entity: 'ane', type: 'n' }
MERGE (n:Root %root%);

MATCH (a:Root %root%), (b:Concept %N6%) MERGE (a)-[:MEANS]->(b);
MATCH (a:Root %root%), (b:Locality %locality%) MERGE (a)-[:USED_AT]->(b);
