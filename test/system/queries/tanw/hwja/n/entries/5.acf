var root = { entity: 'éqa', type: 'n' }
MERGE (n:Root %root%);

MATCH (a:Root %root%), (b:Concept %N5%) MERGE (a)-[:MEANS]->(b);
MATCH (a:Root %root%), (b:Locality %locality%) MERGE (a)-[:USED_AT]->(b);
