var root = { root: 'Ami' }
CREATE (n:%tribe% %root%);

MATCH (a:%tribe% %root%), (b:Construct %noun%) CREATE (a)-[:TYPE_OF]->(b);
MATCH (a:%tribe% %root%), (b:Concept %N1%) CREATE (a)-[:MEANS]->(b);
MATCH (a:%tribe% %root%), (b:Locality %locality%) CREATE (a)-[:USED_AT]->(b);
